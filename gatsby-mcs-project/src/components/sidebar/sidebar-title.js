/** @jsx jsx */
import { jsx } from "theme-ui"

const SideBarTitle = props => {
    return (
        <div
            {...props}
            sx={{
            }}
        />
    )
}

export default SideBarTitle